[project]
name = "maintain-archive"
version = "0.1.0"
description = "Maintains the Terraform provider schema archive."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2,<3",
    "packaging>=23",
]

[tool.uv]
package = false
