
Error: Failed to load plugin schemas

Error while loading schemas for plugin components: Failed to obtain provider
schema: Could not load the schema for provider
registry.opentofu.org/infra-dots/infradots: failed to retrieve schema from
provider "registry.opentofu.org/infra-dots/infradots": 2 problems:

- Invalid Attribute/Block Name: When validating the schema, an implementation
issue was found. This is always an issue with the provider and should be
reported to the provider developers.

"clientId" at schema path "clientId" is an invalid attribute/block name.
Names must only contain lowercase alphanumeric characters (a-z, 0-9) and
underscores (_).
- Error converting resource schema: The schema for the resource
"infradots_workspace" couldn't be converted into a usable type. This is
always a problem with the provider. Please report the following to the
provider developer:

AttributeName("vcs_id"): must have Required, Optional, or Computed set..

