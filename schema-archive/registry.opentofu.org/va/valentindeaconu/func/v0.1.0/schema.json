{
  "provider": {
    "version": 0,
    "block": {
      "attributes": {
        "cache_path": {
          "type": "string",
          "description": "Path to the local cache directory. If not set, it defaults to `$XDG_CACHE_HOME/func/libraries`. Can also be set via an environment variable `FUNC_CACHE_PATH`.",
          "description_kind": "markdown",
          "optional": true
        }
      },
      "block_types": {
        "library": {
          "nesting_mode": "list",
          "block": {
            "attributes": {
              "source": {
                "type": "string",
                "description": "Source of the library file.\n The source of the library file can be any [getter](https://github.com/hashicorp/go-getter#url-format) accepted URL (similar to Terraform module's sources). It can also be set via an environment variable like `FUNC_LIBRARY_{ID}_SOURCE`, where the `{ID}` value can be replaced with anything. The provider doesn't really care about this, as long as it is prefixed with the `FUNC_LIBRARY_` prefix, it will be found and read accordingly.",
                "description_kind": "markdown",
                "required": true
              }
            },
            "description": "Configuration for the functions library.",
            "description_kind": "markdown"
          }
        }
      },
      "description": "The **func** provider is a powerful and unique Terraform provider that enables you to define and execute custom functions. It seamlessly blends functional and declarative paradigms, unlocking new possibilities for managing infrastructure with greater flexibility and efficiency. \nThis provider allows you to define functions in an external, functional language and then use them in your Terraform codebase.",
      "description_kind": "markdown"
    }
  },
  "data_source_schemas": {
    "func": {
      "version": 0,
      "block": {
        "attributes": {
          "id": {
            "type": "string",
            "description": "The name of the function.",
            "description_kind": "markdown",
            "required": true
          },
          "inputs": {
            "type": "dynamic",
            "description": "Inputs to be fed into the function. The inputs can either be a tuple (and the order matters! - equivalent of Python's `args`), or an object with named parameters (the order doesn't matter - equivalent of Python's `kwargs`). For any other type, the provider will throw an error.",
            "description_kind": "markdown",
            "required": true
          },
          "result": {
            "type": "dynamic",
            "description": "The result of the function. The type will be inferred from the function return type.",
            "description_kind": "markdown",
            "computed": true
          }
        },
        "description": "Execute a function via data-source (for pre-v1.8 workflows).",
        "description_kind": "markdown"
      }
    }
  }
}
