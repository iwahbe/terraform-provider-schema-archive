{
  "provider": {
    "version": 0,
    "block": {
      "attributes": {
        "bastion_host": {
          "type": "string",
          "description": "Default bastion/jump host address for multi-hop SSH connections.",
          "description_kind": "markdown",
          "optional": true
        },
        "bastion_key_path": {
          "type": "string",
          "description": "Default path to SSH private key file for bastion host. Falls back to ssh_key_path if not set.",
          "description_kind": "markdown",
          "optional": true
        },
        "bastion_password": {
          "type": "string",
          "description": "Default SSH password for bastion host (sensitive).",
          "description_kind": "markdown",
          "optional": true,
          "sensitive": true
        },
        "bastion_port": {
          "type": "number",
          "description": "Default bastion host SSH port. Defaults to 22.",
          "description_kind": "markdown",
          "optional": true
        },
        "bastion_private_key": {
          "type": "string",
          "description": "Default SSH private key content for bastion host (sensitive). Falls back to ssh_private_key if not set.",
          "description_kind": "markdown",
          "optional": true,
          "sensitive": true
        },
        "bastion_user": {
          "type": "string",
          "description": "Default SSH user for bastion host. Falls back to ssh_user if not set.",
          "description_kind": "markdown",
          "optional": true
        },
        "insecure_ignore_host_key": {
          "type": "bool",
          "description": "Skip SSH host key verification. WARNING: This is insecure and should only be used for testing. Defaults to false. Deprecated: Use strict_host_key_checking = \"no\" instead.",
          "description_kind": "markdown",
          "optional": true
        },
        "known_hosts_file": {
          "type": "string",
          "description": "Path to a custom known_hosts file for SSH host key verification. Defaults to ~/.ssh/known_hosts.",
          "description_kind": "markdown",
          "optional": true
        },
        "ssh_certificate": {
          "type": "string",
          "description": "Default SSH certificate content for certificate authentication. Used with ssh_private_key or ssh_key_path.",
          "description_kind": "markdown",
          "optional": true,
          "sensitive": true
        },
        "ssh_certificate_path": {
          "type": "string",
          "description": "Default path to SSH certificate file for certificate authentication. Used with ssh_private_key or ssh_key_path.",
          "description_kind": "markdown",
          "optional": true
        },
        "ssh_key_path": {
          "type": "string",
          "description": "Default path to SSH private key file. Mutually exclusive with ssh_private_key.",
          "description_kind": "markdown",
          "optional": true
        },
        "ssh_password": {
          "type": "string",
          "description": "Default SSH password for password authentication (sensitive). Use this as an alternative to key-based authentication.",
          "description_kind": "markdown",
          "optional": true,
          "sensitive": true
        },
        "ssh_port": {
          "type": "number",
          "description": "Default SSH port. Defaults to 22.",
          "description_kind": "markdown",
          "optional": true
        },
        "ssh_private_key": {
          "type": "string",
          "description": "Default SSH private key content (sensitive). Mutually exclusive with ssh_key_path.",
          "description_kind": "markdown",
          "optional": true,
          "sensitive": true
        },
        "ssh_user": {
          "type": "string",
          "description": "Default SSH user for connections. Can be overridden per-resource.",
          "description_kind": "markdown",
          "optional": true
        },
        "strict_host_key_checking": {
          "type": "string",
          "description": "SSH host key checking mode. Valid values: \"yes\" (default, requires host key in known_hosts), \"no\" (insecure, skip verification), \"accept-new\" (accept and save new host keys, reject changed keys).",
          "description_kind": "markdown",
          "optional": true
        }
      },
      "description": "\nThe remote_docker_compose_file provider manages Docker Compose files on remote hosts via SSH.\n\nThis provider:\n- Uploads docker-compose files to remote hosts\n- Optionally runs 'docker compose up -d' and 'docker compose down'\n- Tracks file state in Terraform (not container state)\n- Validates compose files before applying\n- Supports bastion/jump hosts for multi-hop SSH\n\n## Example Usage\n\n```hcl\nprovider \"remote-docker-compose-file\" {\n  ssh_user     = \"root\"\n  ssh_key_path = \"~/.ssh/id_ed25519\"\n}\n\nresource \"remote_docker_compose_file_stack\" \"app\" {\n  host        = \"192.168.1.100\"\n  remote_path = \"/opt/stacks/app/docker-compose.yaml\"\n  content     = file(\"docker-compose.yaml\")\n  up          = true\n}\n```\n",
      "description_kind": "markdown"
    }
  },
  "resource_schemas": {
    "remote_docker_compose_file_stack": {
      "version": 0,
      "block": {
        "attributes": {
          "bastion_host": {
            "type": "string",
            "description": "Bastion/jump host address for multi-hop SSH connections.",
            "description_kind": "markdown",
            "optional": true
          },
          "bastion_key_path": {
            "type": "string",
            "description": "Path to SSH private key file for bastion host. Falls back to ssh_key_path if not set.",
            "description_kind": "markdown",
            "optional": true
          },
          "bastion_password": {
            "type": "string",
            "description": "SSH password for bastion host (sensitive).",
            "description_kind": "markdown",
            "optional": true,
            "sensitive": true
          },
          "bastion_port": {
            "type": "number",
            "description": "Bastion host SSH port. Defaults to 22.",
            "description_kind": "markdown",
            "optional": true
          },
          "bastion_private_key": {
            "type": "string",
            "description": "SSH private key content for bastion host (sensitive). Falls back to ssh_private_key if not set.",
            "description_kind": "markdown",
            "optional": true,
            "sensitive": true
          },
          "bastion_user": {
            "type": "string",
            "description": "SSH user for bastion host. Falls back to ssh_user if not set.",
            "description_kind": "markdown",
            "optional": true
          },
          "content": {
            "type": "string",
            "description": "Content of the docker-compose file.",
            "description_kind": "markdown",
            "required": true
          },
          "content_hash": {
            "type": "string",
            "description": "SHA256 hash of the compose file content.",
            "description_kind": "markdown",
            "computed": true
          },
          "host": {
            "type": "string",
            "description": "Remote host address (IP or hostname).",
            "description_kind": "markdown",
            "required": true
          },
          "id": {
            "type": "string",
            "description": "Resource identifier (host:remote_path).",
            "description_kind": "markdown",
            "computed": true
          },
          "insecure_ignore_host_key": {
            "type": "bool",
            "description": "Skip SSH host key verification. WARNING: Insecure. Defaults to false. Deprecated: Use strict_host_key_checking = \"no\" instead.",
            "description_kind": "markdown",
            "optional": true
          },
          "known_hosts_file": {
            "type": "string",
            "description": "Path to a custom known_hosts file for SSH host key verification. Defaults to ~/.ssh/known_hosts.",
            "description_kind": "markdown",
            "optional": true
          },
          "remote_path": {
            "type": "string",
            "description": "Absolute path on the remote host where the compose file should be placed.",
            "description_kind": "markdown",
            "required": true
          },
          "ssh_certificate": {
            "type": "string",
            "description": "SSH certificate content for certificate authentication. Used with ssh_private_key or ssh_key_path.",
            "description_kind": "markdown",
            "optional": true,
            "sensitive": true
          },
          "ssh_certificate_path": {
            "type": "string",
            "description": "Path to SSH certificate file for certificate authentication.",
            "description_kind": "markdown",
            "optional": true
          },
          "ssh_key_path": {
            "type": "string",
            "description": "Path to SSH private key file. Mutually exclusive with ssh_private_key.",
            "description_kind": "markdown",
            "optional": true
          },
          "ssh_password": {
            "type": "string",
            "description": "SSH password for password authentication.",
            "description_kind": "markdown",
            "optional": true,
            "sensitive": true
          },
          "ssh_port": {
            "type": "number",
            "description": "SSH port. Defaults to 22.",
            "description_kind": "markdown",
            "optional": true,
            "computed": true
          },
          "ssh_private_key": {
            "type": "string",
            "description": "SSH private key content. Mutually exclusive with ssh_key_path.",
            "description_kind": "markdown",
            "optional": true,
            "sensitive": true
          },
          "ssh_user": {
            "type": "string",
            "description": "SSH user. Overrides provider default.",
            "description_kind": "markdown",
            "optional": true,
            "computed": true
          },
          "strict_host_key_checking": {
            "type": "string",
            "description": "SSH host key checking mode. Valid values: \"yes\" (default, requires host key in known_hosts), \"no\" (insecure, skip verification), \"accept-new\" (accept and save new host keys, reject changed keys).",
            "description_kind": "markdown",
            "optional": true
          },
          "up": {
            "type": "bool",
            "description": "Run 'docker compose up -d' after uploading. Defaults to false.",
            "description_kind": "markdown",
            "optional": true,
            "computed": true
          },
          "validate": {
            "type": "bool",
            "description": "Validate compose file with 'docker compose config' before applying. Defaults to true.",
            "description_kind": "markdown",
            "optional": true,
            "computed": true
          }
        },
        "description": "\nManages a Docker Compose stack on a remote host via SSH.\n\n## Behavior\n\n- **Create**: Uploads compose file, optionally validates, optionally runs 'docker compose up -d'\n- **Read**: Retrieves remote file hash for drift detection\n- **Update**: If content changes, runs 'docker compose down' (if up=true), uploads new file, runs 'docker compose up -d'\n- **Delete**: Runs 'docker compose down', removes compose file\n\n## Important Notes\n\n- Terraform tracks the **compose file**, not container state\n- The 'up' flag is a convenience operation; containers may drift outside Terraform\n- Remote host must have Docker and 'docker compose' (v2) installed\n\n## Example Usage\n\n```hcl\nresource \"remote_docker_compose_file_stack\" \"web\" {\n  host        = \"192.168.1.100\"\n  remote_path = \"/opt/stacks/web/docker-compose.yaml\"\n  content     = file(\"${path.module}/docker-compose.yaml\")\n  up          = true\n  validate    = true\n\n  ssh_user     = \"deploy\"\n  ssh_key_path = \"~/.ssh/deploy_key\"\n}\n```\n",
        "description_kind": "markdown"
      }
    }
  }
}
